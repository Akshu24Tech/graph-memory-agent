[project]
name = "graph-memory-agent"
version = "0.1.0"
description = "Graph-grounded query agent over the Akshu Mind wiki (Neo4j + GraphRAG)"
requires-python = ">=3.11"
dependencies = [
    "neo4j>=5.20",
    "python-dotenv>=1.0",
    "fastembed>=0.3",
    "google-genai>=0.3",
]
